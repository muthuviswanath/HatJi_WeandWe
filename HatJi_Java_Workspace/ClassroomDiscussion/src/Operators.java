
public class Operators {
	public static void main(String[] args) {
		//Arithmetic: +, -, *, /, %
		// 34 % 12345
		// In case of the numerator is less than the
		//denominator, the result of mod is always the
		// numerator value.
		
		// In case of the numerator is less than the
		//denominator, the result of floor division is always the
		// 0.
		
		// == in case of primitives checks the value
		// in case of non-primitives checks the address
		
		// 2 << 1 (2) * 2
		// 2 << 2 (2) * 4
		// 2 << 3 (2) * 8
		
		// 2 >> 1 (2) / 2 (floor division)
		// 2 >> 2 (2) / 4 (floor division)
		// 2 >> 3 (2) / 8 (floor division)
		
		// 0010
		// 0100
	}
}


// While: If we unsure of the number of iterations
// For: If the number of iterations are known
// Syntactical difference

/*
 * int i=0, j=0, k=;
 * while(i>=0){
 * System.out.print("Hi");
 * }
 * 
 * 
 * */
